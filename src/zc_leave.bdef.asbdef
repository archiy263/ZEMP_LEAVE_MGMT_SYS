projection;
strict ( 2 );
use draft;

define behavior for ZC_LEAVE alias Leave
{

  use create;
  use update;
  use delete;

  use action Approve;
  use action Reject;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}