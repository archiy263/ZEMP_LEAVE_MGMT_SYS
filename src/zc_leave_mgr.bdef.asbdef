projection;
strict ( 2 );
use draft;

define behavior for ZC_LEAVE_MGR alias Leave
{
  use action Approve;
  use action Reject;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}